#include <stdio.h>
#include <stdlib.h>

int main(int argc, char* argv[]) {
	printf("you can mod nl2 steps below!\n");
	printf("go into the newest release of the gtmgf2 pack downloaded via google drive now go into the nl2_data then go into modding then there's steps on modding in unity in the readme.txt file!");
	getchar();
	return 0;
}